# Fresh Codex Session Prompt

Project path: `D:\godot后室`

You are continuing the Backrooms Godot 4.6.2 Mobile-renderer prototype. Do not rely on chat history only. Start by reading these files in order:

1. `README.md`
2. `CURRENT_STATE.md`
3. `docs/AGENT_START_HERE.md`
4. `docs/PROGRESS.md`
5. `docs/DECISIONS.md`
6. `docs/FORBIDDEN_PATTERNS.md`
7. `docs/ACCEPTANCE_CHECKLIST.md`
8. `docs/HANDOFF_20260504_PROC_MAZE.md`

Before edits, run:

```powershell
git status --short
git diff --stat
```

This folder is now a git repository after the GitHub sync. Run those commands and record meaningful environment changes in `CURRENT_STATE.md`.

Current accepted working target:

- Playable proc-maze scene: `scenes/tests/Test_ProcMazeMap.tscn`
- No-ceiling full-map preview: `scenes/tests/Test_ProcMazeMap_NoCeilingPreview.tscn`
- Playable launcher: `run_proc_maze_test.bat`
- Preview launcher: `run_proc_maze_no_ceiling_preview.bat`
- MVP verification-room launcher: `run_mvp_room.bat`
- Monster showcase/global transform launcher: `run_monster_showcase.bat`
- Texture/UV tool launcher: `start_texture_tool.bat`
- Godot launchers resolve the local Godot install through `_godot_env.bat`; do not hardcode another user's `C:\Users\...\Godot...` path.
- Latest layout evidence: `artifacts/screenshots/test_proc_maze_layout.png`
- Playable proc-maze lighting controls: press `ESC` in `run_proc_maze_test.bat`; click outside the panel to return to captured gameplay.
- Monster source edits should go through `run_monster_showcase.bat` or direct edits under `scenes/mvp/FourRoomMVP.tscn/MonsterRoot`. Saving in the showcase updates the global source transform lines in `FourRoomMVP.tscn`.

Current proc-maze validation summary:

- seed: `2026050401`
- generator_version: `proc_maze_fixed_layout_v0.10_door_reveal_clearance`
- rooms: `36`
- main path: `18`
- branches: `8`
- loops: `4`
- dead ends: `4`
- long corridors: `5`
- L-turn spaces: `2`
- L-shaped rooms: `4`
- internal large rooms: `4`
- hubs: `3`
- ordinary rectangular rooms: `5`
- special reserves: `2`
- narrow corridors: `8`
- normal corridors: `5`
- normal-width spaces: `14`
- large-width spaces: `6`
- hub-width spaces: `3`
- reachable: `true`
- overlap: `false`
- door_to_wall: `false`
- door_reveal_blocker: `false`
- active light fixtures: `28`
- active real light sources: `38`

Important production rules:

- Do not modify `scenes/mvp/FourRoomMVP.tscn` for proc-maze tests unless the user explicitly asks.
- Proc-maze spaces must be generated by module type, not by room-specific wall edits.
- Non-rectangular rooms must use `shape_cells`; validators and builders must use occupied cells, not only bounding rectangles.
- Corridors must stay visibly narrower than rooms. Current width tiers are `narrow_corridor=2.5m`, `normal_corridor=5.0m`, `normal_room=7.5m`, and `large_room/hub_room=10.0m`.
- Do not use long rectangular room modules to fake corridors. Current required corridor module IDs are `corridor_narrow_straight`, `corridor_long_straight`, `corridor_l_turn`, `corridor_t_junction`, and `corridor_offset`.
- Do not improve perceived complexity by adding chains of small square rooms or short connector chunks. Prefer true corridors, L rooms, wide rooms, hubs, or compound large rooms.
- Do not create non-passable decorative slits. L-shaped rooms must use occupied-cell L footprints and boundary walls only; internal L-room baffles are forbidden.
- Compound large-room internal passages currently use `INTERNAL_PASSAGE_WIDTH = 1.60`.
- Shared MVP/proc-maze ambient baseline is currently `WORLD_AMBIENT_ENERGY = 0.07`.
- Proc-maze does not require one light fixture per space. Narrow corridors, L-turn corridors, T junctions, offset corridors, and short non-long corridors may be intentionally unlit. Ceiling light panels must fit the owner occupied-cell footprint and must not overlap walls/internal partitions.
- Long rectangular lamp panels must use distributed real light sources along the panel length, not one full-strength center point. Current metrics use `active_light_count=28` visible fixtures and `active_light_source_count=38` real `OmniLight3D` sources.
- Door frames must not open directly into an abrupt wall or full-height internal partition. Proc-maze openings now own a doorway reveal buffer; internal partitions are trimmed around it, and `has_door_reveal_blocker=false` is required.
- Current validator rejects 3 ordinary rectangular rooms in a row, 3 short connector spaces in a row, ordinary rectangle ratio above 35%, missing area anchor rooms, missing macro-loop split/merge clarity, and too many long corridors.
- Do not use negative scale, mirror scale, one-off stretched walls, or per-room `Room_A/Room_B/...` exceptions.
- Do not reintroduce large transparent masks, black/gray cover planes, or whole-wall visibility fades.
- If a visual experiment is needed, make a copy/experiment scene first; only merge to base after user visual acceptance.

Current visual-material baseline:

- The 2026-05-04 reference-style texture/trim pass was reverted at the user's request.
- Current accepted look is the pre-reference-style baseline: restored wall and door-frame PNGs, regenerated uniform floor tiles, flat ceiling material, and previous warm flat emissive light panels.
- `SceneBuilder.gd` and `ProcMazeSceneBuilder.gd` should not generate `Baseboard` / `Trim` nodes in the current baseline.
- Room ceiling-light panels are back to `Vector3(1.2, 0.08, 0.7)`.
- Latest player-view visual evidence is `artifacts/screenshots/test_proc_maze_map.png`.
- Do not reapply generated reference wallpaper, baseboards, speckled ceiling textures, or long diffuser panels unless the user explicitly asks to try that direction again.
- Direct user texture edits should go through `start_texture_tool.bat`, which writes shared material/texture files and backs up overwritten textures under `materials/textures/_texture_tool_backups/`.
- The texture tool includes a WebGL `模型实时预览` section that applies the selected material to a simple wall/floor/ceiling/door/light 3D sample and updates live as UV/color/emission inputs change. It supports drag rotation and wheel zoom.
- The texture tool has local Explorer buttons for project-contained material/texture source folders only.
- The texture tool also has `图层合成`: it keeps a base albedo snapshot, lets the user upload multiple candidate overlay textures per layer, and can randomly place/scale/blend those overlays into the existing material albedo slot. Layer assets live under `materials/textures/texture_tool_layers/`; layer settings are stored in `codex_tools/texture_tool/texture_layers.json` when saved.
- Current root launchers should stay minimal: `run_proc_maze_test.bat`, `run_proc_maze_no_ceiling_preview.bat`, `run_mvp_room.bat`, and `start_texture_tool.bat`.
- The playable proc-maze scene now has `Systems/LightingTuningPanel`, backed by `scripts/lighting/LightingTuningPanel.gd`, for runtime control of light color, energy, range, attenuation, lamp-panel emission, ambient color/energy, and flicker enable. It must not be copied into the no-ceiling preview or used as permission to modify `scenes/mvp/FourRoomMVP.tscn`.

Useful commands:

```powershell
$godot=(Get-Command Godot_v4.6.2-stable_win64_console.exe -ErrorAction Stop).Source
& $godot --headless --path . --script res://scripts/tools/BakeTestProcMazeMap.gd
& $godot --headless --path . --script res://scripts/tools/ValidateTestProcMazeMap.gd
& $godot --headless --path . --script res://scripts/tools/ValidateProcMazePlayable.gd
& $godot --headless --path . --script res://scripts/tools/BakeTestProcMazeNoCeilingPreview.gd
& $godot --headless --path . --script res://scripts/tools/ValidateProcMazeNoCeilingPreview.gd
& $godot --headless --path . --script res://scripts/tools/CaptureTestProcMazeMapLayout.gd
```

Texture tool:

```powershell
.\start_texture_tool.bat
python codex_tools\texture_tool\texture_tool_server.py --self-test
.\run_mvp_room.bat
```

Known non-blocking issue:

- Headless viewport screenshots can fail or hang because Godot uses a dummy texture in headless mode. For screenshot capture scripts, use non-headless mode with a bounded `--quit-after`; keep `CaptureTestProcMazeMapLayout.gd` for reliable headless layout evidence.

Next recommended task:

1. User visually accepts or rejects whether corridors now feel narrow enough in `run_proc_maze_test.bat` and `run_proc_maze_no_ceiling_preview.bat`.
2. If accepted, implement randomized topology using the same width-tier `ModuleRegistry`, `MapValidator`, occupied-cell generation, and `SceneValidator`.
3. If rejected, tune fixed layout/module type quotas first. Do not hand-place walls or use room modules as corridors.
