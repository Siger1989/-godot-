# HANDOFF

## 2026-04-29 交接

### 仓库

- GitHub remote: `https://github.com/Siger1989/-godot-.git`
- 项目目录: `backrooms_mobile_demo`
- Godot 版本: 4.3 stable

### 当前状态

- 主 Demo 可运行，入口为 `scenes/main/Main.tscn`。
- 主角已替换为用户提供的 GLB：`assets/models/player.glb`。
- GLB 包含骨骼和 1 个 `Armature` 动画片段。
- 已接入人物状态动作：idle、walk、run。
- 跑步时前臂已做轻微压低修正。
- 右侧 `人物展示` 面板可调整人物高度、朝向、上下、左右、前后偏移。
- 天花板半透明面片已默认关闭，避免拉远镜头闪烁。
- 门关闭后的房间记忆状态已处理，不再显示实时灯板和动态内容。
- 小场景验证过的关门记忆规则已同步到主游戏 `RoomPrototypeSection`。
- 主游戏中已访问但不可见房间只显示静态结构记忆，不显示 `light_mesh`、真实灯光和 detail/dynamic 内容。
- 小场景房间关系已同步到主游戏：开门邻房不直接切 `PARTIAL_VISIBLE`，未进入前保持 `UNKNOWN`。
- `OutOfBoundsVoid` 视觉已隐藏，仅保留碰撞，避免灰色大块遮挡画面。

### 验证

- `validate_all.bat` 已通过。
- 关键结果包括：
  - `MAIN_SCENE_OK`
  - `PLAYER_MODEL_OK`
  - `MAIN_CLOSED_MEMORY_OK`
  - `FOG_TEST_OK`
  - `ROOM_PROTOTYPE_OK`
  - `VISIBILITY_BLEND_OK`
  - `VISIBILITY_CLOSED_MEMORY_OK`

### 下次继续

- 回家后 clone 仓库，先读 `CURRENT_STATE.md` 和本文件。
- 运行 `run_game.bat` 查看主 Demo。
- 如要继续视觉调参，优先调整人物展示窗口、地板亮度、门框和记忆房间显示。
- 每次完成一轮修改后，更新本文件或 `CURRENT_STATE.md`，提交并推送。
- 每次交付时给出当前可启动入口：默认使用 `run_game.bat`；需要分享给别人时再导出 Windows/Web 包。

## 2026-04-29 方案调整

- 用户确认：先不要继续在当前主游戏结构上硬改。
- 后续方案：以目前已经敲定的小场景为基础继续延申，把小场景中连续、稳定的空间关系作为新主线。
- 当前上传目的：保留现有进度，回头先对齐整体方案，再决定是否重建主游戏场景结构。

## 2026-04-30 小场景视觉连续性

- 已修小场景 `Visibility_Blend_Test` 的可见结构连续性。
- 物理视线能看到的地板/墙/踢脚线/转角件现在保留原始贴图，不再被 `UNKNOWN` 状态刷成黑色。
- 转角柱改为同墙布材质、缩小尺寸，用于弱化墙布转角黑边。
- 同类 bug 已继续处理：基础结构不再使用黑灰状态材质，大墙块可见性改为多点采样，灯板/灯光改为平滑权重，避免范围边界瞬间亮起。
- 已进一步修正物理视线遮挡：当前房间结构不再绕过射线强制实时显示；遮挡后基础结构保持静态记忆贴图，灯和动态内容不进入记忆，只平滑淡出。
- 摄像机挡墙透明度改为平滑过渡，减少移动时闪烁。
- 摄像机切墙时新增轻微深色切线，只作为设计辅助边界，不纳入记忆系统和遮挡系统。
- 灯光和灯板显示已拆开：灯光可以照亮玩家可见墙面，即使灯板本体被遮挡；灯板被拆成多段，只显示视线能看到的部分。记忆房间仍关闭灯光和灯板。
- 地板和墙面已从大块拆为小块可见性单元，不再主要依赖房间状态隐藏；小块根据物理射线显示实时画面或灰暗记忆，并绘制玩家视野多边形切线作为真实视野边界。
- 墙面小块可见性已改为采样朝向玩家的墙面表面，避免采样点落在墙体中心导致墙板被自身碰撞误挡、整片变灰黑。
- 未知/遮挡墙面的兜底材质保持灰暗墙布，不再隐藏成黑洞；本轮提高了灰暗墙布亮度，减少黑块感。
- 验证：`validate_all.bat` 通过，并重新生成 `screenshots/visibility_blend_initial.png`、`screenshots/visibility_blend_door_reveal.png`、`screenshots/visibility_blend_room_b_memory.png`。

## 2026-04-30 物理视线灯光回归修正

- 已修正小场景里灯光重新变成“房间触发”的问题：`VISITED` 状态不再直接关掉视线内的灯板和真实灯。
- 灯板可见性、真实灯开关统一由水平物理视线决定；玩家走到走廊后仍能看到身后的 RoomA 灯，返回初始点后仍能看到走廊灯。
- 关门后的已访问房间仍保持静态记忆：灯板、真实灯和动态内容关闭，不出现实时变化。
- 新增回归验证到 `ValidateVisibilityBlendTest.gd`，覆盖初始点、走廊、返回初始点的灯光连续性。
- 验证：`validate_all.bat` 通过，关键结果包含 `VISIBILITY_BLEND_OK` 和 `VISIBILITY_CLOSED_MEMORY_OK`。

## 2026-04-30 小场景结构可见性修正

- 已修正玩家走到走廊后，身后房间突然整片变黑的问题。
- 基础结构显示不再按整房间 `VISITED` 兜底：墙/地板块只有被物理视线真正看见过，才会在失去视线后进入灰暗静态记忆。
- 实时可见的墙/地板块直接显示原贴图，只在视野边缘保留很轻的过渡混色，避免低权重可见面看起来像整房间变黑。
- 真实灯光不再由“当前房间可见”直接打开整房间，而是由物理可见的地板/灯板驱动。
- 收紧水平射线容差：墙面采样保留小容差避免自遮挡，地板和灯板使用严格容差；地板采样点从边缘向块内收，减少门边误判。
- 小场景门板宽度调整为 `1.56`，补上关门时的门缝，避免门后地板误判可见、导致门后真实灯残留。
- 验证：`validate_all.bat` 通过；闭门验证输出 `light_target=0.000 floor=0.000 lamp=0.000`。

## 2026-04-30 视野切线与灰色记忆

- 视野切线改为只在物理视野距离跳变处绘制柔和直线，不再沿整圈视野多边形绘制硬黑边。
- 已看过但当前被遮挡的墙/地板块显示为灰色静态记忆，并使用 unshaded 材质，不参与实时灯光变化。
- 从未看过的墙/地板块不再用灰暗墙布兜底，直接隐藏为全黑未知区域。
- 验证：`validate_all.bat` 通过，并重新生成小场景截图。

## 2026-04-30 盲区墙面误亮修正

- 墙面实时可见判断增加“墙前方探测点”检查：墙面本体被射线打中不再足够，墙前方空间也必须处在物理视野内。
- 墙/踢脚线/墙边件需要多个采样点同时可见才切到实时贴图，避免门边或墙角单个采样点漏光导致整片盲区墙面变亮。
- 墙面自遮挡容差收紧到 `0.035`，地板和灯板仍保持严格容差。
- 验证：`validate_all.bat` 通过，并重新生成小场景截图。

## 2026-04-30 盲区灰色记忆柔化

- 实时亮面和已见记忆进一步分离：弱可见或刚进入遮挡的墙/地板块不再直接跳到黑色，而是先进入稍暗灰色静态记忆。
- 灰色记忆亮度上调，墙/地板比正常实时画面略暗，但不参与实时灯光，避免灯光变化导致记忆区域闪烁。
- 未被记录为见过的结构仍保持隐藏，即全黑未知区。
- 记忆探测只对尚未记录的结构执行，避免每帧重复射线开销。
- 验证：`validate_all.bat` 通过。

## 2026-04-30 灯光影响可见区域

- 真实灯光和灯板 mesh 进一步解耦：灯板被墙遮挡时可以不可见，但真实 `Light3D` 仍可照亮玩家当前能看到的地面/墙面。
- `VisibilityBlendTest` 记录上一帧物理视野多边形，`VisibilityBlendSection` 的灯会检测自身是否能通过无遮挡射线照到这些可见地面采样点。
- 关闭门后的记忆房间仍不会残留实时灯光，闭门验证保持 `light_target=0.000 floor=0.000 lamp=0.000`。
- 验证：`validate_all.bat` 通过。

## 2026-04-30 挡墙透现范围柔化

- 镜头到玩家之间的挡墙透明区改为半径渐变：命中点周围 `2.45m` 内的挡墙按距离淡出，不再只透明单块墙板。
- 中心透明度降到 `0.34`，边缘逐步恢复到 `0.94`，淡入淡出时间拉长到 `0.34s`，减少突兀跳变。
- 摄像机切墙线透明度降低，并且只在中心透明区域显示，边缘过渡处不再画硬边。
- 验证：`validate_all.bat` 通过；当前无头截图脚本拿到空 viewport，只做空图保护，实际效果以启动窗口目测为准。

## 2026-04-30 灯光可达与遮罩切线修正

- 真实灯光改为检测灯到玩家可见地面区域的水平物理路径，灯板本体被遮挡或不在玩家视线里时，只要灯能照到可见地面，`Light3D` 仍保持开启。
- 地板记忆不再整块深色压暗，已见但遮挡区域只保留很轻的静态色差；真实视野边界改为更宽、更淡的连续遮罩线，减少地板块状马赛克。
- 镜头挡墙淡出改为附近墙体统一透明，不再按墙板距离给不同 alpha；同时关闭每块墙自己的竖向切线，减少人物在墙后时看到一节一节的墙板拼缝。
- 验证：`validate_all.bat` 通过，新增 `VISIBILITY_BLEND_open_door_light_reaches_visible_floor` 回归检查。
